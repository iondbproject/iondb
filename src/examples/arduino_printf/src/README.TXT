This folder contains sample code that demonstrates the use of the
serial_c_iface.cpp/.h which allows c code called from an Arduino
sketch the ability to use a printf statement and have the output
redirected to the serial0 port.

***IMPORTANT***
The library files in serial_io with the example directory are not live. 
The most recent copy will need to be retrieved from /src/serial_io.